[phases.setup]
nixPkgs = ["nodejs-20_x"]

[phases.install]
cmds = ["npm install --prefix bch-agent-app"]

[phases.build]
cmds = ["echo 'Skipping frontend build for API deployment'"]

[start]
cmd = "node bch-agent-app/api-server.js"
